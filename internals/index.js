module.exports = {
    request : require('./request'),
    response : require('./response')

    /*
    ...require('./request'), //so this will exports all those events which we have done in request.js
    ...require('./response') //so this will exports all those events which we have done in response.js
    */
}